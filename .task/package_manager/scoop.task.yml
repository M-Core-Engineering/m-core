version: '3'

tasks:
  install:
    internal: false
    cmds:
      - scoop install {{.PKG}}
    status:
      - scoop which {{.PKG}}

  update:
    internal: false
    cmds:
      - scoop update {{.PKG}}