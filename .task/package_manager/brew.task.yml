version: '3'

tasks:
  install:
    internal: true
    cmds:
      - brew install {{.PKG}}
    status:
      - brew list {{.PKG}} > /dev/null 2>&1

  update:
    internal: true
    cmds:
      - brew upgrade {{.PKG}}

  remove:
    internal: true
    cmds:
      - brew uninstall {{.PKG}}